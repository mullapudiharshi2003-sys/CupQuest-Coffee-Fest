# CupQuest GitHub Pages
Upload all files and the `assets` folder to the repository root. Enable Pages from `Settings > Pages > Deploy from a branch > main > /(root)`.

To change the UPI ID, edit the first line of `app.js`. This static version stores cart data on each phone and does not verify payment automatically.
